{
  "name": "molgang-bridge",
  "version": "1.0.0",
  "description": "MOLGANG Roblox ↔ Web Game bridge (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.52.0"
  }
}
