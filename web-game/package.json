{
  "name": "molgang-web-game",
  "version": "1.0.0",
  "description": "MOLGANG Web Game — Three.js HD renderer with XRPL/Hedera NFT inventory",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "@types/three": "^0.168.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
